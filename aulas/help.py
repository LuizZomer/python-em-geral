def soma(a,b):
    """
    A definição soma pega o argumento 'a' e 'b' e soma, printando na tela.    
    """
    s = a+b
    print(s)

soma(3,4)

help(soma) #A docstrigs(as 3 aspas duplas) cria um manual da sua função, que pode ser acessado usando a função help